{
  "name": "whatsapp-chat-analyzer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.cjs",
    "cf:dev": "npm run build && npx wrangler@latest pages dev dist",
    "cf:deploy": "npm run build && npx wrangler@latest pages deploy"
  },
  "dependencies": {
    "@google/genai": "^2.20.0",
    "dotenv": "^17.4.2",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
